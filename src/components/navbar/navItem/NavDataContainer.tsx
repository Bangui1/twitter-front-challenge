import styled from "styled-components";
import {StyledContainer} from "../../common/Container";


export const NavDataContainer = styled(StyledContainer)`

    @media (max-width: 600px) {
        display: none;
    }
`;
