

interface SendIconProps {
    onClick: () => void;
}

const SendIcon = (
    {
        onClick,
    }: SendIconProps) => {

    return (
        <div>

        </div>
    );

};

export default SendIcon;
